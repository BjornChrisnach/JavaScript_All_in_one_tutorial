// @ts-check
{
    var name = prompt();
    if(name === "Caleb")    //identity operator // strict equality
    {
        console.log("Welcome!");
    }
    else if(name === "Sally")
    {
        console.log("Welcome Sally!");
    }
}